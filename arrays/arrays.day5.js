const strings = ["a", "b", "c", "d"]
// 4*4 = 16 bytes of storage
// we stored it, lets grab it
strings[2] // hey you stored a DS in memory called strings which is an array. grab the 3rd value or 2nd index from this array